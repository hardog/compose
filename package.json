{
  "name": "koa-compose",
  "description": "compose Koa middleware",
  "repository": "koajs/compose",
  "version": "2.4.0",
  "keywords": [
    "koa",
    "middleware",
    "compose"
  ],
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "co": "^4.6.0",
    "mocha": "^2.4.5",
    "should": "^2.0.0",
    "istanbul": "^0.4.5"
  },
  "scripts": {
    "test": "node_modules/mocha/bin/mocha --harmony --require should --reporter spec test/test@co4.js",
    "test-cov": "node --harmony ./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha -- --require should",
    "test-travis": "node --harmony ./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- --require should"
  },
  "license": "MIT"
}
